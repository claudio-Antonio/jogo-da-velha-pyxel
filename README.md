Para começar, crie uma venv:
  - python3 -m venv .venv
Entre na venv
  - source .venv/bin/activate
Uma vez na venv, baixe as dependências 
  - `pip install -r requirements.txt`
Crie o arquivo .env com esse conteúdo
      `IP=192.168.1.1`
      `PORTA_SERVER=1234'1`

